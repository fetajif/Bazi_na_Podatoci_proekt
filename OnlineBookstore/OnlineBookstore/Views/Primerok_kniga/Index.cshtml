@model IEnumerable<OnlineBookstore.Models.primerok_kniga>

@{
    ViewBag.Title = "Index";
}

<h2>Primerocite na knigi</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.isbn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cena)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.kosnicka_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.magacin_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.isbn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cena)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.kosnicka_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.magacin_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.primerok_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.primerok_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.primerok_id })
        </td>
    </tr>
}

</table>
